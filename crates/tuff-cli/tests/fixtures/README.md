# Test fixtures

This directory is reserved for controlled inputs used by the integration
tests. Fixtures should cover test scenarios such as malformed manifests,
legacy lockfiles, conflicts, and other edge cases. User-facing capability
demonstrations belong under `examples/<type>/`.
