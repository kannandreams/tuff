[package]
name = "tuff-core"
version = "0.1.0"
edition = "2024"
description = "Core lifecycle engine and adapter contract for Tuff."
license = "MIT"

[dependencies]
tuff-hooks-spec = { path = "../tuff-hooks-spec" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
sha2 = "0.10.9"
git2 = { version = "0.20", features = ["vendored-libgit2"] }
tempfile = "3.23.0"
toml = "0.9.8"
url = "2.5.6"
