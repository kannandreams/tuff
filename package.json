{
  "name": "coral-root",
  "private": true,
  "scripts": {
    "docs:start": "npm --prefix website run dev",
    "docs:build": "npm --prefix website run build",
    "docs:serve": "npm --prefix website run preview",
    "docs:typecheck": "npm --prefix website run check"
  }
}
