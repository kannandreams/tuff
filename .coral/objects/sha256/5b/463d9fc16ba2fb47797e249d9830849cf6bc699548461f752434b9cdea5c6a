#!/bin/sh

# Keep startup hooks quick and predictable. This example only reports the
# project directory; replace it with a project-specific check as needed.
printf '%s\n' "Coral Claude session started in $(pwd)"
